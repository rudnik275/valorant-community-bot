# publisher/

Publisher-tick: cron job that runs every 1 minute, reads pending Detected events,
and posts them to the Telegram chat respecting Anti-spam quota.

Will be implemented in issue #9 (publisher-tick).
